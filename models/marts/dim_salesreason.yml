models:
  - name: dim_salesreason
    description: >
      asdasdasd
    columns:
      - name: salesreason_pk
        description: Unique identifier for each customer.
        tests:
          - unique
          - not_null