models:
  - name: dim_product
    description: >
      asdasdasd
    columns:
      - name: product_pk
        description: Unique identifier for each customer.
        tests:
          - unique
          - not_null