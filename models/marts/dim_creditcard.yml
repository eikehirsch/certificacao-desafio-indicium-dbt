models:
  - name: dim_creditcard
    description: >
      asdasdasd
    columns:
      - name: creditcard_pk
        description: Unique identifier for each customer.
        tests:
          - unique
          - not_null