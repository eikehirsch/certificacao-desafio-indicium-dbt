models:
  - name: dim_creditcard
    description: >
      A table that lists and details credit cards, including information such as credit card type.
    columns:
      - name: creditcard_pk
        description: Unique identifier for each credit card.
        tests:
          - unique
          - not_null

      - name: cardtype
        description: The type of the credit card.